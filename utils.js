const getNames = (list) => list;
const getSeniors = (list) => list;
const getRegionCounts = (list) => list;
const convertDateStamps = (list) => list;

module.exports = {
  convertDateStamps,
  getNames,
  getSeniors,
  getRegionCounts,
};
